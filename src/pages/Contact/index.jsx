import React from 'react'
import * as Styled from './Contact.styled'

export const Contact = ({id}) => {
  return (
    <Styled.Wrapper
        id={id}
    >
        <span>Contact</span>
    </Styled.Wrapper>
  )
}